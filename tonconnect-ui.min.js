// [Mock] TonConnectUI simulation
class TonConnectUI {
  constructor(config) {
    console.log('TonConnectUI initialized with manifest:', config.manifestUrl);
  }
  async connectWallet() {
    alert('TON кошелек подключен (симуляция)');
  }
  get connected() {
    return { account: { address: 'EQFAKEADDRESS1234567890' } };
  }
}